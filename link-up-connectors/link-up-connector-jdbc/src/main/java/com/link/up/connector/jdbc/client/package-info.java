/**
 * JDBC client-side connection helpers.
 *
 * <p>This package owns connection-level interaction that does not belong to
 * catalog metadata discovery or row read/write roles.</p>
 */
package com.link.up.connector.jdbc.client;
