/**
 * Worker use cases and control-plane orchestration.
 *
 * <p>Application code coordinates domain state through ports. It must not own
 * local threads, semaphores, executor services, HTTP servlet types, or concrete
 * infrastructure adapters.</p>
 */
package com.link.up.server.application;
