/**
 * Connector discovery and preparation owned by the Link-Up framework.
 *
 * <p>This package contains the canonical runtime {@code FactoryRegistry} and
 * resolved connector resources used by planning. It may discover and prepare
 * connector implementations but must not schedule or execute tasks.
 */
package com.link.up.framework.connector;
